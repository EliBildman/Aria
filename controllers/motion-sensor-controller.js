

module.exports.initialize = () => {
    //
}

module.exports.set_parameters = (params) => {
    //send parameters to device
}

module.exports.take_new_connection = (connection) => {
    //called from router
}