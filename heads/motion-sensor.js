

const initialize = () => {
    //
}

const set_parameters = (params) => {
    //send parameters to device
}

const take_new_connection = (connection) => {
    //called from router
}

module.exports = {
    name: 'MotionSensor',
    initialize,
    events: [],
    actions: [],
}